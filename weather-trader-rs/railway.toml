[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
region = "us-east4"  # Closest to Kalshi (AWS us-east-1)
startCommand = "/app/weather-trader"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/"
port = 8080
